- name: copy {{componenet}} services
  ansible.builtin.copy:
    src: catalogue.service 
    dest: /etc/systemd/system/{{component}}.service

- name: deamon realod and start
  ansible.builtin.service:
    daemon-reload: true

- name: start and enable {{component}}
  ansible.builtin.service:
    name: catalogue
    state: restarted
    enabled: yes